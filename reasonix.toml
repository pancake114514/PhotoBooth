[permissions]
allow = ["Bash=git -C \"F:\\IDEProjects\\IdeaProjects\\Photobooth\" status --short; git -C \"F:\\IDEProjects\\IdeaProjects\\Photobooth\" log --oneline -5 2>$null", "Edit", "Bash=node -v; npm -v; npm config get registry; git --version", "Bash=$vswhere = \"${env:ProgramFiles(x86)}\\Microsoft Visual Studio\\Installer\\vswhere.exe\"; if (Test-Path $vswhere) { & $vswhere -latest -property installationPath } else { \"VS not found\" }; $cl = Get-Command cl -ErrorAction SilentlyContinue; if ($cl) { \"cl: \" + $cl.Source } else { \"cl not found (no MSVC in PATH)\" }", "Bash=Get-ChildItem -Force tmp-scaffold | ForEach-Object { Move-Item -Path $_.FullName -Destination . -Force }; Remove-Item -Recurse -Force tmp-scaffold; Get-ChildItem -Force | Select-Object Name", "Bash=npm install 2>&1", "Bash=npm install better-sqlite3 sharp exifr 2>&1", "Bash=npx electron verify-native.cjs 2>&1"]
